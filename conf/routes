# RoutesGET    /api/v1/callme               contorllers.RestController.
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /                           controllers.Application.index()
POST    /api/v1/addCompany          controllers.RestController.addCompany()
POST    /api/v1/startIdentification controllers.RestController.startIdentification()
GET     /api/v1/identifications     controllers.RestController.identifications()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

GET    /api/v2/index                    controllers.EmployeeController.index()
GET    /api/v2/addNewNode              controllers.EmployeeController.addNewNode()
GET    /api/v2/FindNewNode/:node        controllers.EmployeeController.FindNewNode(node: String)


